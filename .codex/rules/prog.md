# Progress Log

- 2026-05-17 00:41:26 CST: 接手 session 019e30c9-f5c3-7772-99fb-541bf97b70d6 后的 umi zarr 到 lerobot parquet 转换排查；确认当前未见残留转换进程，开始检查脚本、输出目录和 episode_000195.parquet 状态。
- 2026-05-17 00:44:55 CST: 检查 LeRobot 输出目录；确认 meta/info.json 记录 196 episodes / 147180 frames，episodes.jsonl 与 episodes_stats.jsonl 各 196 行；episode_000195.parquet 可被 pyarrow 正常读取，838 行，与源 zarr 最后一集长度一致。
- 2026-05-17 00:45:30 CST: 汇总全部 196 个 parquet 的 metadata 行数，合计 147180 行，与 LeRobot info.json 和源 zarr camera0_rgb 帧数一致；判断转换已完成，无需续跑。
- 2026-05-17 00:48:40 CST: 检查 `.codex` 和仓库文本，未发现已关闭 session 的旧 plan；在 `.codex/rules/plan.md` 写入基于当前状态重建的后续计划。
- 2026-05-17 00:53:08 CST: 在 `~/.codex/sessions` 找到已关闭 session `019e30c9-f5c3-7772-99fb-541bf97b70d6` 的 rollout，恢复最后一版 “UMI Conveyor 微调 pi0/pi05 执行计划”，并用当前实际 `/data/archive_liyixuan23/finetune_pi` 路径更新 `.codex/rules/plan.md`。
- 2026-05-17 00:54:27 CST: 开始执行 norm stats 阶段；确认 `assets/` 下无现有 UMI norm stats。首次按旧位置参数调用失败，改为 `--config-name`；随后发现 `/data/.../openpi_cache` 在当前 sandbox 不可写，因此把已有 `paligemma_tokenizer.model` 复制到仓库内 `.openpi_cache` 作为可写 OPENPI_DATA_HOME。
- 2026-05-17 01:04:31 CST: 完成 `pi0_umi_ur5e_lora` 完整 norm stats 计算，写入 `assets/pi0_umi_ur5e_lora/liyixuan23/umi_conveyor/norm_stats.json`。因 compute 脚本仅统计 UMI repack/data transform 后的 `state` 和 `actions`，pi0/pi05 结果相同，复制到 `assets/pi05_umi_ur5e_lora/liyixuan23/umi_conveyor/norm_stats.json`，并验证两个 config 均可加载 `state/actions` norm stats。
- 2026-05-17 01:05:40 CST: 按训练规则运行 `nvidia-smi`，GPU 1-3 均空闲；选择 GPU 1 启动 tmux session `pi0_umi_smoke`，执行 `pi0_umi_ur5e_lora` 1-step smoke training，日志写入 `logs/pi0_umi_smoke.log`。
- 2026-05-17 01:08:56 CST: 用户补充规则：特别大的 checkpoint 应下载到 `/data/archive_liyixuan23/finetune_pi`。停止 `pi0_umi_smoke`，避免继续使用仓库 `.openpi_cache`；发现 `pi0_base` 已下载完成约 12G，准备迁移到 `/data/archive_liyixuan23/finetune_pi/openpi_cache` 并更新后续训练环境变量。
- 2026-05-17 01:12:04 CST: 已将 `.openpi_cache/openpi-assets` 迁移到 `/data/archive_liyixuan23/finetune_pi/openpi_cache/openpi-assets`，并复制 tokenizer 小缓存；仓库 `.openpi_cache` 从约 12G 降至约 4MB。后续训练改用 `OPENPI_DATA_HOME=/data/archive_liyixuan23/finetune_pi/openpi_cache`。
- 2026-05-17 01:16:52 CST: 重启 `pi0_umi_smoke` 后确认从 `/data/.../openpi_cache/openpi-assets/checkpoints/pi0_base/params` 恢复权重；训练随后因 UMI `action_dim=7` 与官方 base `action_dim=32` 的 `action_in_proj` 形状不匹配失败。新增 `ShapeCompatibleCheckpointWeightLoader`，使 UMI pi0/pi05 config 加载形状兼容的 base 权重，并对不兼容动作头和 LoRA 新参数保留随机初始化。
- 2026-05-17 01:21:10 CST: `pi0_umi_ur5e_lora` 1-step smoke training 通过，写出 `checkpoints/pi0_umi_ur5e_lora/smoke/0`，日志显示 `loss=2.0420`、`grad_norm=24.4392`；复查 GPU 1-3 均空闲，准备继续 `pi05_umi_ur5e_lora` smoke。
- 2026-05-17 01:26:50 CST: `pi05_umi_ur5e_lora` 1-step smoke training 通过，`pi05_base` 下载/缓存位置为 `/data/archive_liyixuan23/finetune_pi/openpi_cache/openpi-assets/checkpoints/pi05_base`，写出 `checkpoints/pi05_umi_ur5e_lora/smoke/0`，日志显示 `loss=1.1999`、`grad_norm=2.2743`。
- 2026-05-17 01:28:20 CST: 更新 `.codex/rules/plan.md` 的 Current Execution State，记录 norm stats、shape-compatible loader、pi0/pi05 smoke checkpoint 和大 checkpoint 缓存位置；当前剩余阶段为正式长训练。
- 2026-05-17 01:29:36 CST: 检查仓库 `.hf_cache` 和 `.openpi_cache`：`.hf_cache` 中 8.8G 为 HuggingFace datasets Arrow cache，不是模型 ckpt，但已迁到 `/data/archive_liyixuan23/finetune_pi/hf_cache` 并将仓库 `.hf_cache` 改为 symlink；`.openpi_cache` 仅剩 4.1M tokenizer，确认 `/data/.../openpi_cache` 已有同一份后也改为 symlink，原小目录备份到 `/tmp/openpi_cache_repo_backup_20260517_0129`。
- 2026-05-17 01:39:18 CST: 跑完 `pi0_umi_ur5e_lora` 和 `pi05_umi_ur5e_lora` 的 10-step batch16 单卡 benchmark。GPU 为 RTX 6000 Ada 48G；两者显存峰值观察约 44.2G/49.1G。pi0 编译后进度约 4.0-4.7s/step；pi05 约 5.0s/step。确认训练代码支持 JAX data parallel/FSDP，但在仅使用 GPU 1-3 时 device_count=3，`fsdp_devices` 只能取 1 或 3，且 global batch 必须能被 3 整除。
- 2026-05-17 01:51:26 CST: 将 pi0/pi05 UMI 正式训练命令和缓存/输出/wandb 规则写入 `.codex/rules/common-rule.mdc`；按用户要求启动 `pi0_umi_2gpu` 两卡 data-parallel 正式训练（GPU 1,2，batch16，wandb online，checkpoint base `/data/archive_liyixuan23/finetune_pi/checkpoints`）。wandb run: `https://wandb.ai/iivyleeee-tsinghua-university/openpi/runs/ihrnnlck`；进入稳态后 GPU 1/2 均 100% util，约 1.1-1.2s/step，预计 30k steps 约 9.5-10h。
- 2026-05-17 02:13:03 CST: 确认当前两卡正式训练在 global batch 16 下每卡显存约 44.3G/49.1G，因此直接升到 global batch 32（每卡 16）大概率显存不够；当前 `save_interval=1000`，已在仓库创建 `data/outputs -> /data/archive_liyixuan23/finetune_pi/checkpoints` 软链接。补充 `scripts/train.py`，后续新启动的训练会额外向 wandb 记录 `lr`、`steps_per_sec`、`samples_per_sec`。
- 2026-05-17 11:25:49 CST: 创建 `start.md`，整理全新机器从已有 raw UMI zarr zip 开始，到 LeRobot 转换、norm stats、smoke training、正式 pi0/pi05 LoRA 训练、缓存路径和云盘容量估算的完整流程；默认新机器没有任何 gitignored 数据，也没有 `/data/archive_liyixuan23/finetune_pi` 内容。
